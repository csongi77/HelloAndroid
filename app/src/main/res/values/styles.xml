<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Udacity style -->
    <style name="udacity" parent="Base.TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@android:color/holo_blue_light</item>
        <item name="android:paddingTop">@dimen/udacity_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/udacity_padding_vertical</item>
        <item name="android:paddingRight">@dimen/udacity_padding_horizontal</item>
        <item name="android:paddingLeft">@dimen/udacity_padding_horizontal</item>
    </style>
    <!-- info text style -->
    <style name="info_text">
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:paddingTop">@dimen/vertical_padding</item>
        <item name="android:paddingBottom">@dimen/vertical_padding</item>
        <item name="android:paddingRight">@dimen/horizontal_padding</item>
        <item name="android:paddingLeft">@dimen/horizontal_padding</item>

    </style>
</resources>
